# Ignore everything by default
*

# Only include the pre-built dx bundle output (built in CI before docker build),
# staged per architecture as dist/<arch> so a multi-platform build can pick the
# binary compiled for the platform it is building.
!dist/
